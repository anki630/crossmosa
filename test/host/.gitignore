# Build output
smbhost
fat_time_test
hid_report_map_test
ble_button_latch_test
memory_arena_test
warm_identity_test
*.o

# Python virtualenv for smb_smoke_test.py's smbprotocol dependency (see
# README.md's "Installing smbprotocol" section) -- deliberately separate
# from the PlatformIO build virtualenv, never commit it.
.venv/

# Root directory HalStorage resolves paths under at runtime (SMBHOST_ROOT,
# defaults to ./sdroot) -- test scratch data, not source.
sdroot/

# Debugging artifacts from investigating this harness (strace output, server
# stdout/stderr redirected to a file, background PID files) -- not source.
*.log
*.pid
